# Reflexion Log

Written by the `/loop-debug` command. Each entry records a failed debug attempt
so the next iteration avoids the same approach. Clear this file when starting a
new (unrelated) debug session.

Format:
## [YYYY-MM-DD] Bug: <short description>
### Attempt N
What tried: …
What failed: …
Hypothesis for next attempt: …

---
(no entries yet — log accumulates during debug sessions)
