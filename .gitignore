# secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem

# deps / build
tools/scraperai/.venv/
__pycache__/
node_modules/
.next/
dist/
build/
.vercel/

# os / editor noise
.DS_Store
*.log
tmp/

# codegraph local index (rebuilt per machine)
.codegraph/

# scratch
_probe_*

# scraped MovieMaps pages and output — regenerate with tools/scraperai/harvest.py.
# The recipes the model produced are tracked; the 24k pages it replays are not.
tools/scraperai/.cache/
data/moviemaps/*.ndjson

# Obsidian per-user state (shared config stays tracked)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Graphify output (regenerated locally, optional)
graphify-out/

# operator handover notes — local only, not for the public/team repo
HANDOVER.md
.vercel
